FORMAT: 1A

# Golemio API

<img src="https://storage.golemio.cz/golemiocz/images/golemioapi_logos.png" alt="Logos" />

**Toto je dokumentace REST API projektu pražské datové platformy Golemio.**

Veškeré dotazy prosím směřujte na [golemio@operatorict.cz](mailto:golemio@operatorict.cz?subject=golemioapiv1)

Dokumentace je v anglickém jazyce. Více informací o projektu naleznete na [www.golemio.cz](https://www.golemio.cz/).

## Description

**This is documentation of RESTful API of Golemio Prague Data Plaform project.**

Support is avalailable via [golemio@operatorict.cz](mailto:golemio@operatorict.cz?subject=golemioapiv1)

More about project Golemio at [www.golemio.cz](https://www.golemio.cz/).

## Versions

- **v0.1 (current)** <!-- __DATE__ -->

## General info

### Authorization

Generate your own token at [api.golemio.cz/api-keys](https://api.golemio.cz/api-keys).

**Use the token as value of `X-Access-Token` header.**

### Usage

#### Pagination

**The API returns maximum count of 10000 objects / rows for one request. You can specify lower number by adding `?limit` URL parameter.**

This may vary depending on the specific route, read the description of the parameters for each route.
You can only get more data using multiple reqeusts with `?limit&offset` parameters, read examples below.

#### Requests rate

**By default, each API key has a rate-limit set of 10000 requests per 10 seconds.**

#### Open Data

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

This badge means it is available for public use as Open Data.

### General responses

| Response code | Description |
| - | - |
| `403: Forbidden` | Your API-Key don't have the necessary rights to the specific route (or specific route+parameters combination). Permissions are explicitly granted for each API-key per route. |
| | API can also return `403` to a call with invalid parameters (for example, you can have permission to view only `route/?type=1` and try to call `route/?type=2`, you can get a `403`)
| `404`: Not found | Non-existing routes or not-found resource returns. |
| `429: Too Many Requests` | API can also return  if you exceed you rate-limit (again, rate-limits are explicitly granted for each API-key individually). |
| `200`: Success | Otherwise success responses |

### General Parameters

Following parameters are applicaple for most of the resoures/routes. All parameters are optional. Don't copy-paste example API calls with all parameters if you don't need them.

| Parameter | Description |
| - | - |
| `latlng` | location, API performs geo-spatial search to retrieve data close to the location (sorts data by proximity); Latitude and Longitude separated by comma, latitude first |
| `range` | filters the results by their distance from provided latlng in meters (range query); is ignored when latlng is not specified |
| `districts` | filters by Prague city districts (in array format - eg. `districts[]=praha7&districts[]=praha6`) |
| `limit` | pagination - limits number of retrieved items (kNN query) |
| `offset` | pagination - mumber of the first items that are skipped |
| `updatedSince` | filters all results with lower `updated_at` than this parameter, in ISO8601 format |
| `ids` | filters by individual entity IDs (in array format - eg. `ids[]=1&ids[]=3`) |

All parameters are optional and are evaluated at the same time as AND (only results meeting all the conditions at the same time are returned).

Multiple values of the same parameter of type array are evaluated as OR (results meeting at least one condition are returned). Eg. `districts` or `ids`.

### Possible headers

| header | description |
| - | - |
| `Cache-Control` | Server-side caching is used by default. If this request header value is set to `no-cache`, always fresh data are served and the cache is not used. |
| `If-None-Match` | Weak E-Tag implementation is used for additional caching. | API returns `ETag` response header, example: W/"21cf-vMBh8gdzFfYJ4qSGaPA6/dooVV0" | If the `If-None-Match` request header value matches the current `ETag`, 304 response (cached) is sent. ETag value changes everytime data are changed. This may save data traffic (as the same unchanged data are not sent) but is not faster on the server side. You should not need this unless you're repeatedly querying the same data and trying to save network traffic. |

## Example requests

| URL | description |
| - | - |
| `/parkings/?limit=10&offset=10` | get 10th - 20th record (2nd page of 10 elements each when paginating) (using `limit` and `offset` parameters) |

👉 More examples at [operator-ict.gitlab.io/golemio/documentation/en/tutorials/golemioapi](https://operator-ict.gitlab.io/golemio/documentation/en/tutorials/golemioapi/)

# Group Air Quality 🌦

## Air Quality Stations CHMI [/airqualitystations]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Air Quality CHMI Stations [GET /airqualitystations/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Air Quality Station)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Air Quality Stations CHMI History [GET /airqualitystations/history/{?from,to,limit,offset,sensorId}]

+ Parameters

    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + from: `2019-05-16T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime
    + to: `2019-05-18T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime
    + sensorId: `12345` (optional, string) - Limits data measured up by sensor with this id

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Air Quality Station History)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Meteosensors TSK [/meteosensors]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Meteosensors TSK [GET /meteosensors/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters out all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Meteosensor)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Meteosensor TSK [GET /meteosensors/{id}]

+ Parameters

    + id: 531801 (number) - Identifier of the meteosensor.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Meteosensor)

+ Response 404 (application/json; charset=utf-8)

    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

# Group Parking 🅿️

## Parking Lots [/parkings]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

| Enum value | Parking type description (cs) | Parking type description (en) |
| - | - | - |
| 1 | P+R parkoviště | P+R parking |
| 2 | placené parkoviště | paid private parking |

### GET All Parking Lots [GET /parkings/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Parking)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Parking Lots History [GET /parkings/history/{?from,to,limit,offset,sensorId}]

+ Parameters

    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + from: `2019-05-16T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime
    + to: `2019-05-18T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime
    + sensorId: `12345` (optional, string) - Limits data measured up by sensor with this id (parameter `id` of the parking)

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Parkings History)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Parking Lot [GET /parkings/{id}]

+ Parameters

    + id: 534017 (number) - Identifier of the parking.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Parking)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Parking Zones [/parkingzones]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

GeoJSON format of all polygons for on-street parking zones. Tariffs for each zone with valid prices per hour in CZK.

| Category code | Category description (cs) | Category description (en) |
| - | - | - |
| RES | rezidentní zóna (modrá) | residential area (blue) |
| VIS | návštěvnická zóna area (oranžová) | visitor area (orange) |
| MIX | smížená zóna area (fialová) | mixed area (purple) |
| KOM | zóna pro ZTP (šedá) | area only with ZTP or forbidden (grey) |

### GET All Parking Zones [GET /parkingzones/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

       + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Polygon)
                + properties (Parking Zone)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Parking Zone [GET /parkingzones/{parkingzoneCode}]

+ Parameters

    + parkingzoneCode: `P1-0101` (string) - Identifier of the parking zone.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Polygon)
        + properties (Parking Zone)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Parking Zones Tariffs [GET /parkingzones/tariffs]

+ Request (application/json; charset=utf-8)

    + Headers

            x-api-key: eyJhbGciOiJIUzUxMiIsInR5cCI6IkpXVCJ9...

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Parking Zone Tariff)


### Parking Spaces [/parking]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

New parking endpoint, currently to be used for KORID Liberec data only.

#### GET All Parking Spaces [GET /parking/{?latlng,range,source,sourceId,limit,offset}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + source: `korid` (optional, string) - Filter by data provider.
    + sourceId: `1` (optional, string) - Filter by parking group id. Unique per data provider.
    + limit: 10 (optional, number) - Limits number of retrieved items.
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Polygon)
                + properties (Parking Space)

+ Response 403 (application/json; charset=utf-8)

    + Attributes (403 Error)

#### GET Parking Space [GET /parking/{id}]

+ Parameters

    + id: `6` (string, required) - Id of parking location.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Polygon)
        + properties (Parking Space)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)

    + Attributes (403 Error)

#### GET All Parking Spaces Measurements [GET /parking/measurements{?sourceId,from,to,limit,offset,source}]

+ Parameters

    + source: `korid` (optional, string) - Filter by data provider.
    + sourceId: `1` (optional, string) - Filter by parking group id. Unique per data provider.
    + from: `2021-02-26T12:01:00.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime.
    + to: `2021-02-26T12:03:00.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime.
    + limit: 10 (optional, number) - Limits number of retrieved items.
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Parking Measurement)

+ Response 403 (application/json; charset=utf-8)

    + Attributes (403 Error)

#### GET All Parking Spaces Tariffs [GET /parking/tariffs/{?sourceId}]

+ Parameters

    + source: `korid` (optional, string) - Filter by data provider.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Parking Space Tariff)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)

    + Attributes (403 Error)

#### GET Parking Space Tariff [GET /parking/tariffs/{tariffId}]

+ Parameters

    + tariffId: `b184865e-5a5e-5465-abc1-4fab6bc56a77` (string, required) - Id of parking tariff.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Parking Space Tariff)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)

    + Attributes (403 Error)

# Group Public Space 🏡

## Gardens [/gardens]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Gardens [GET /gardens/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Garden)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Garden [GET /gardens/{id}]

+ Parameters

    + id: `frantiskanska-zahrada` (string) - Identifier of the garden.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Garden)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET All Gardens Properties [GET /gardens/properties]

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Garden Property)

## Medical Institutions [/medicalinstitutions]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Medical Institutions [GET /medicalinstitutions/{?latlng,range,districts,group,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, string) - Filter by Prague city districts (slug) separated by comma.
    + group: `pharmacies` (optional, enum[string]) - Filter by group of types of the medical institution.
        + Members
            + `pharmacies`
            + `health_care`
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Medical Institution)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Medical Institution [GET /medicalinstitutions/{id}]

+ Parameters

    + id: `2526713200064203-fakultni-nemocnice-v-motole` (string) - Identifier of the medical institution.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Medical Institution)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET All Medical Institution Types [GET /medicalinstitutions/types]

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Body

            {
                "pharmacies": [
                    "Nemocniční lékárna s odbornými pracovišti",
                    "Lékárna",
                    "Lékárna s odbornými pracovišti",
                    "Výdejna",
                    "Vojenská lékárna",
                    "Vojenská lékárna s OOVL",
                    "Nemocniční lékárna",
                    "Lékárna s odbornými pracovišti s OOVL",
                    "Nemocniční lékárna s OOVL",
                    "Nemocniční lékárna s odbor. pracovišti s OOVL",
                    "Odloučené oddělení výdeje léčivých přípravků",
                    "Lékárna s OOVL",
                    "Lékárna s odborným pracovištěm, která zásobuje lůžková zdravotnická zařízení",
                    "Lékárna s odborným pracovištěm, která zásobuje lůžková zdravotnická zařízení s OOVL",
                    "Lékárna zásobujicí zařízení ústavní péče",
                    "Lékárna zásobujicí zařízení ústavní péče s OOVL"
                ],
                "health_care": [
                    "Fakultní nemocnice",
                    "Nemocnice",
                    "Ostatní ambulantní zařízení",
                    "Ostatní zdravotnická zařízení",
                    "Zdravotní záchranná služba",
                    "Zdravotnické středisko"
                ]
            }

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Municipal Authorities [/municipalauthorities]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Municipal Authorities [GET /municipalauthorities/{?latlng,range,districts,type,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, string) - Filter by Prague city districts (slug) separated by comma.
    + type: `municipality` (optional, enum[string]) - Filter by group of types of the municipal authorities.
        + Members
            + `municipality`
            + `city-hall`
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Municipal Authority)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Municipal Authority [GET /municipalauthorities/{id}]

+ Parameters

    + id: `urad-mestske-casti-praha-10` (string) - Identifier of the municipality.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Municipal Authority)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET All Municipal Authority Waiting Queues [GET /municipalauthorities/{id}/queues]

+ Parameters

    + id: `skoduv-palac` (string) - Identifier of the municipality.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Municipal Authority Queues)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Municipal Police Stations [/municipalpolicestations]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Municipal Police Stations [GET /municipalpolicestations/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Municipal Police Station)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Municipal Police Station [GET /municipalpolicestations/{id}]

+ Parameters

    + id: 72 (number) - Identifier of the police station.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Municipal Police Station)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Playgrounds [/playgrounds]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Playgrounds [GET /playgrounds/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Playground)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Playground [GET /playgrounds/{id}]

+ Parameters

    + id: 72 (number) - Identifier of the playground.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Playground)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET All Playgrounds Properties [GET /playgrounds/properties]

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Playground Property)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Prague City Districts [/citydistricts]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Districts [GET /citydistricts/{?latlng,range,limit,offset,updatedSince}]

+ Parameters


    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Polygon)
                + properties (City District)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET District [GET /citydistricts/{id}]

+ Parameters

    + id: `praha-1` (string) - Identifier or slug of the city district.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Polygon)
        + properties (City District)


+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Public Toilets [/publictoilets]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Public Toilets [GET /publictoilets/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Public Toilet)


+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Public Toilet [GET /publictoilets/{id}]

+ Parameters

    + id: 72 (number) - Identifier of the public toilet.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Public Toilet)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Municipal Libraries [/municipallibraries]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Municipal Libraries [GET /municipallibraries/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Municipal Library)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Municipal Library [GET /municipallibraries/{id}]

+ Parameters

    + id: 2 (number) - Identifier of the library.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Municipal Library)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)


# Group Public Transport 🚋

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

GTFS ([General Transit Feed Specification](//gtfs.org)) data about the city's public transportation schedules in form of an API.
Publisher of the data is ROPID. Schedules are valid for upcoming ~12 days, but can change as they are updated every day.

More about at [pid.cz/o-systemu/opendata](//pid.cz/o-systemu/opendata/).

## GTFS Services [/gtfs/services]

### GET GTFS Services [GET /gtfs/services{?limit,offset,date}]

+ Parameters
    + date: `2019-01-01` (optional, string) - Filter services by specific date in format (YYYY-MM-DD).
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array[GTFS Service])

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## GTFS Routes [/gtfs/routes]

### GET All GTFS Routes [GET /gtfs/routes/{?limit,offset}]

+ Parameters
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array[GTFS Route])

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET GTFS Route [GET /gtfs/routes/{id}]

+ Parameters
    + `id`: `L991` (string, required)

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (GTFS Route)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## GTFS Trips [/gtfs/trips]

### GET All GTFS Trips [GET /gtfs/trips/{?limit,offset,stopId,date}]

+ Parameters

    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + stopId: U953Z102P (optional, string) - Filter trips that include specific stop.
    + date: `2018-01-01` (optional, string) - Filter trips by specific date.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (GTFS Trip)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET GTFS Trip [GET /gtfs/trips/{id}{?includeShapes,includeStops,includeStopTimes,includeService,includeRoute,date}]

+ Parameters
    + `id`: `991_1151_190107` (string, required)
    + includeShapes: true (optional, boolean) - Enrich trip response object with shapes.
    + includeStops: true (optional, boolean) - Enrich trip response object with stops.
    + includeStopTimes: true (optional, boolean) - Enrich trip response object with stops times.
    + includeService: true (optional, boolean) - Enrich trip response object with service.
    + includeRoute: true (optional, boolean) - Enrich trip response object with routes.
    + date: `2019-01-01` (optional, string) - Filter trips by specific date in format (YYYY-MM-DD).

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (GTFS Trip)
        + stop_times (array[GTFS Stop time])
        + shapes (array)
            + (Feature Point)
                + properties (GTFS Shape)
        + service (GTFS Service)
        + route (GTFS Route)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## GTFS Shapes [/gtfs/shapes/{id}]

### GET GTFS Shape [GET]

+ Parameters
    + `id`: `L991V2` (string, required) - GTFS shapes_id to filter shapes by GTFS trip

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (GTFS Shape)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## GTFS Stops [/gtfs/stops]

### GET All GTFS Stops [GET /gtfs/stops/{?names,ids,aswIds,cisIds,limit,offset}]

+ Parameters
    + names: `Prosecká` (optional, string) - Name of station. Use with square brackets `names[]=NAME1&names[]=NAME2`.
    + ids: `U476Z103P` (optional, string) - Get stops by stop_id. Use with square brackets `ids[]`.
    + aswIds: `1833_12` (optional, string) - Get stops by ASW, use `_` instead of `/` or encode the slash sign with `%2F`. Use with square brackets `aswId[]`.
    + cisIds: `12345` (optional, number) - Get stops by CIS Id. Use with square brackets `cisId[]`.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (GTFS Stop)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET GTFS Stop [GET /gtfs/stops/{id}]

+ Parameters
    + `id`: `U476Z104P` (string, required)

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (GTFS Stop)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## GTFS Stop times [/gtfs/stoptimes]

### GET GTFS Stop times [GET /gtfs/stoptimes/{id}{?limit,offset,from,to,date,includeStop}]

+ Parameters
    + `id`: `U118Z101P` (string, required)
    + date: `2019-01-01` (optional, string) - Filter stop times by specific date in format (YYYY-MM-DD).
    + from: `07:12:33` (optional, string) - Filter stop times by time departure in format (H:mm:ss).
    + to: `12:12:33` (optional, string) - Filter stop times by time arrival in format (H:mm:ss).
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + includeStop: true (optional, boolean) - Enrich stop times response object with stop.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array[GTFS Stop time])

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## RealTime Vehicle Positions [/vehiclepositions]

💡 Response comes with special header `X-Last-Modified` with timestamp value of the latest resource update.
When you use it as `?updatedSince` query parameter you will obtain only newer data than this value.

### GET All Vehicle Positions [GET /vehiclepositions{?limit,offset,includeNotTracking,includePositions,cisTripNumber,preferredTimezone,routeId,routeShortName,updatedSince}]

+ Parameters

    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + includeNotTracking: true (optional, boolean) - Enrich result dataset with not tracking trip (those trips not being on track - before and after trip)
    + includePositions: true (optional, boolean) - Enrich result dataset with positions
    + cisTripNumber: 1020 (number, optional) Filter result by CIS trip number
    + preferredTimezone: `Europe%2FPrague` (string, optional) - Preferred timezone offset by given Country/City (use encoded slash sign with `%2F` or use `_` sign)
    + routeId: L301 (string, optional) Filter result by route id
    + routeShortName: 381 (string, optional) Filter result by short name
    + updatedSince: `2020-04-07T06:32:29.000Z` (string, optional) Limit results to the ones updated after (timestamp greater than)

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Vehicleposition Compound)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Vehicle Position [GET /vehiclepositions/{gtfsTripId}{?includeNotTracking,includePositions,preferredTimezone}]

+ Parameters

    + gtfsTripId: `115_107_180501` (string, required) Filter result by trip id (gtfs_trip_id)
    + includeNotTracking: true (optional, boolean) - Enrich result dataset with not tracking trip (last tracked vehicle after its trip)
    + includePositions: true (optional, boolean) - Enrich result dataset with positions
    + preferredTimezone: `Europe%2FPrague` (string, optional) - Preferred timezone offset by given Country/City (use encoded slash sign by %2F or use _ sign)

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Vehicleposition Compound)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## GTFS RealTime Vehicle Positions [/vehiclepositions/gtfsrt]

### GET GTFS RealTime Trip Updates Protocol Buffer [GET /vehiclepositions/gtfsrt/trip_updates.pb]

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/octet-stream)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET GTFS RealTime Vehicle Positions Protocol Buffer [GET /vehiclepositions/gtfsrt/vehicle_positions.pb]

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/octet-stream)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Departure Boards [/departureboards]

You have to use query params `ids`, `cisIds` and `aswIds` in array format - eg. `ids[]=1&ids[]=3` when **at least one of these ids is required**. The maximum is 100 stops combined for one request.

### GET Departure Board [GET /departureboards/{?ids,cisIds,aswIds,minutesBefore,minutesAfter,names,preferredTimezone,orderBySchedule,showAllRoutesFirst,limit,offset}]

+ Parameters

    + ids: `U476Z103P` (optional, string) - Get result by stop_id (GTFS, id of specific stop)
    + aswIds: `1833_12` (optional, string) - Get result by ASW, use `_` instead of `/` or encode the slash sign with `%2F`
    + cisIds: `12345` (optional, number) - Get result by CIS Id
    + minutesBefore: `10` (optional, number) - Get departures relative before to request time. Default is set to 0.
    + minutesAfter: `60` (optional, number) - Get departures relative after the request time. Default is set to 180.
    + names: `Prosecká` (optional, string) - Name of station. Use with square brackets `names[]=NAME1&names[]=NAME2`.
    + preferredTimezone: `Europe%2FPrague` (string, optional) - Preferred timezone offset by given Country/City (use encoded slash sign with `%2F` or use _ sign)
    + orderBySchedule: `false` (optional, boolean) - Order by schedule, not by real delays
    + showAllRoutesFirst: `false` (optional, boolean) - Sort by all distinct route - trip headsign first and then by arrival time.
    + limit: 20 (optional, number) - Limits number of retrieved items. The maximum is 1000 (default value is 20).
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)
    can return empty data (empty array []) if no departures were found
    + Attributes (array)
        + (Departure Board)

+ Response 404 (application/json; charset=utf-8)
    returns 404 if no stop was not found (by the stop_id) at all
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)


## PID Departure Boards [/pid]

![golemioapi-version-badge](https://img.shields.io/badge/in%20beta-version-blue)

You have to use query parameters `ids`, `cisIds`, `aswIds` or `names` in array format - eg. `ids[]=1&ids[]=3`. **At least one of these IDs is required.** The maximum is 100 stops combined in one request.

### GET Departure Board [GET /pid/departureboards/{?ids,aswIds,cisIds,names,minutesBefore,minutesAfter,timeFrom,preferredTimezone,orderBySchedule,showAllRoutesFirst,mode,limit,offset}]

+ Parameters

    + ids: `U458Z101P` (optional, string) - Get result by GTFS stop_id. Can be used to retrive individual stops and to separate departures of Prague and intercity routes, even if they depart from the same physical stop. A list of GTFS stops can be found in `stops.txt` file of [GTFS feed](//opendata.praha.eu/dataset/jizdni-rady-pid).
    + aswIds: `458_101` (optional, string) - Get result by ASW ID. First part of the number represents the whole node. Usually it groups the stops of the same name or all stops associated with a metro station. The second part of the number represents individual stops in the node. Use `_` instead of `/` as a separator or encode the slash sign with `%2F`. A list of ASW IDs can be found in [Prague Open data](//opendata.praha.eu/dataset/zastavky-a-oznacniky-pid).
    + cisIds: `28004` (optional, number) - Get result by CIS ID. A list of CIS IDs can be found in [Prague Open data](//opendata.praha.eu/dataset/zastavky-a-oznacniky-pid).
    + names: `Smíchovské nádraží` (optional, string) - Get results by exact name of stop (case and whitespace sensitive). May return stops of the same name in different towns.
    + minutesBefore: `10` (optional, number) - Set the start of interval from which to retrieve departures. Positive numbers are set in past relative to the time of request or `?timeFrom` timestamp, negative numbers set the start in the future (use to compensate for walking distance to a stop). Default is set to 0.
    + minutesAfter: `60` (optional, number) - Set the end of interval from which to retrieve departures. Positive numbers are set in future relative to the time of request or `?timeFrom` timestamp, negative are in the past. The sum of minutesBefore and minutesAfter must be larger than zero. Default is set to 180.
    + timeFrom: `2021-01-21T06:00:00Z` (string, optional) - Set initial timestamp for time interval given by `?minutesBefore&minutesAfter`. Use ISO8601 time format (use encoded signs: `%3A` as `:`, `%2B` as `.`, `%2F` as `+`). Timezone offset example `Z` for UTC, `+02:00` for CEST.
    + preferredTimezone: `Europe%2FPrague` (string, optional) - Preferred timezone offset as defined in IANA Time zone database in the form of Country/City (use encoded slash sign with `%2F` or use _ sign)
    + orderBySchedule: `false` (optional, boolean) - Order results by scheduled time, not by predicted time.
    + showAllRoutesFirst: `false` (optional, boolean) - First return departures for every distinct route and headsign, then fill the rest ordered by departure time.
    + mode: `departures` (optional, string) - Select mode to show only `departures`/`arrivals`/`mixed` results.
    + limit: 20 (optional, number) - Limits the number of retrieved items. The maximum is 1000 (default value is 20).
    + offset: 0 (optional, number) - Number of the initial departures that are skipped. Useful for multi-page displays.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)
    Can return empty data in departures property (empty array []) if no departures were found
    + Attributes (PID Departure Board)

+ Response 404 (application/json; charset=utf-8)
    Returns 404 if no stop was not found (by the stop_id) at all
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

# Group Traffic 🚘

## Shared Cars  [/sharedcars]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

| Enum value | Fuel description (cs) | Fuel description (en) |
|- |- |- |
| 1 | benzin | gas |
| 2 | nafta | diesel |
| 3 | LPG | LPG |
| 4 | elektřina | electric |
| 5 | hybrid | hybrid |
| 6 | CNG |CNG  |
| 7 | jiný | other |
| 0 | neznámý | unknown |

| Enum value | Availability description (cs) | Availability description (en) |
|- |- |- |
| 1 | ihned | now |
| 2 | dle domluvy s provozovatelem | upon the agreement with provider |

### GET All Shared Cars [GET /sharedcars/{?latlng,range,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

   + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Shared Car)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Shared Car [GET /sharedcars/{id}]

+ Parameters

    + id: `1BF8210` (string) - Identifier of the car.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Shared Car)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)


## Shared Bikes [/sharedbikes]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

| Enum value | Type description (cs) | Type description (en) |
|- |- |- |
| 1 | kolo | bike |
| 2 | elektrokolo | ebike |
| 3 | skútr | scooter |

### GET All Shared Bikes [GET /sharedbikes/{?latlng,range,limit,offset,updatedSince,companyName}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter
    + companyName: HOMEPORT (optional, string) - Filters all results with company.name property by this parameter.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

   + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Shared Bike)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Shared Bike [GET /sharedbikes/{id}]

+ Parameters

    + id: `homeport-0076` (string) - Identifier of the bike.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Shared Bike)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Bicycle Parkings [/bicycleparkings]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Bicycle Parkings [GET /bicycleparkings/{?latlng,range,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

   + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Bicycle Parking)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Bicycle Parking [GET /bicycleparkings/{id}]

+ Parameters

    + id: `282743091` (number) - Identifier of the bicycle parking.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Bicycle Parking)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Bicycle Counters [/bicyclecounters]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

| Vendor | Vendor id prefix | Measurement time aggregation | Temperature availability |
| - | - | - | - |
| Camea | `camea-` | 5 minutes | available |
| EcoCounter | `ecoCounter-` | 15 minutes | not available |

⚠️ Measurements of vendor Camea are every day at 5:00am (UTC) validated and recalculated and can change then.

### GET All Bicycle Counters [GET /bicyclecounters/{?latlng,range,limit,offset}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Bicycle Counter)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

+ Response 400
    + Attributes (Error)

### GET Bicycle Counters Detections [GET /bicyclecounters/detections{?from,to,id,aggregate,limit,offset,id}]

+ Parameters
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + from: `2019-05-16T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime
    + to: `2019-05-18T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime
    + aggregate: `true` (optional, boolean) - sum aggregated by directions_id is returned
    + id: `camea-BC_ZA-BO` (array[string]) - direction ids



+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Bicycle Counter Detections)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

+ Response 400
    + Attributes (Error)


### GET Bicycle Counters Temperatures [GET /bicyclecounters/temperatures{?from,to,id,aggregate,limit,offset,id}]

+ Parameters
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + from: `2019-05-16T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime
    + to: `2019-05-18T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime
    + aggregate: `true` (optional, boolean) -  average aggregated by locations_id is returned
    + id: `camea-BC_ZA-KLBO` (array[string]) - location ids

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
     + (Bicycle Counter Temperatures)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

+ Response 400
    + Attributes (Error)

## Traffic Cameras [/trafficcameras]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Traffic Cameras [GET /trafficcameras/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

   + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Traffic Camera)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Traffic Camera [GET /trafficcameras/{id}]

+ Parameters

    + id: 404032 (number) - Identifier of the camera.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Traffic Camera)

+ Response 404 (application/json; charset=utf-8)
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)


# Group Waste ♻️

## Sorted Waste Stations [/sortedwastestations/]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

💡 About 400 containers are equiped with IoT sensors that measure their fullness. Use `?onlyMonitored` parameter to retrieve only these monitored containers.
More about this project at [Chytrý svoz odpadu](https://www.smartprague.eu/projekty/chytry-svoz-odpadu).

**Station accessibility**

| Enum value | Description (cs) | Description (en) |
| - | - | - |
| 1 | volně | accessible |
| 2 | obyvatelům domu | only for house residents |
| 3 | neznámá dostupnost | unknown |

**Trash type**
| Enum value | Description (cs) | Description (en) |
| - | - | - |
| 1 | Barevné sklo | Tinted glass |
| 2 | Elektrozařízení | Electric waste |
| 3 | Kovy | Metals |
| 4 | Nápojové kartóny | Beverage cartons |
| 5 | Papír | Paper |
| 6 | Plast | Plastics |
| 7 | Čiré sklo | Clear glass |
| 8 | Textil | Textiles |

**Container types**

| Values |
| - |
| 1100 NPU Okřínek SV |
| 1100 mini H SV |
| 1100 normální HV |
| 120 normální HV |
| 1500 M 1,5 SV |
| 2000 NPU Okřínek SV |
| 2150 MEVA SV |
| 240 normální HV |
| 2500 Reflex SV |
| 3000 Podzemní SV |
| 3200 Reflex SV |
| 3200 Schaffer SV |
| 3300 GFA DUO SV |
| 3350 Atomium Reflex SV |
| 4000 Podzemní SV |

**Cleaning Frequency**

| Value | 1st digit - period duration | 2nd digit - frequency | Description |
| - | - | - | - |
| `13` | `1` | `3` | 3 times per 1 week |
| `61` | `6` | `1` | Once per 6 weeks |

### GET All Sorted Waste Stations [GET /sortedwastestations/{?latlng,range,districts,accessibility,limit,offset,onlyMonitored}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, string) - Filter by Prague city districts (slug) separated by comma.
    + accessibility: 1 (optional, string) - Filter by accessibility id.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + onlyMonitored: true (optional, boolean) - Filter only stations with at least one "smart" container with sensor for measurements and pickups

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Sorted Waste Station)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Sorted Waste Station [GET /sortedwastestations/station{?id}]

+ Parameters

    + id: `0a292077-4dbd-501a-ac60-d11c80e889c5` (optional, string) - Main identifier of the station.
    + knskoId: `15288` (optional, string) - KNSKO identifier of the station.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Sorted Waste Station)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Sorted Waste Fullness Sensors Data [/sortedwastestations]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

💡 About 400 containers are equipped with IoT sensors that measure their fullness. Use these endpoints to get measurements from these devices.
More about this project at [Chytrý svoz odpadu](https://www.smartprague.eu/projekty/chytry-svoz-odpadu).

### GET All Sorted Waste Measurements [GET /sortedwastestations/measurements/{?containerId,limit,offset,from,to,knskoId}]

+ Parameters

    + containerId: `1a90b0e0-fabb-5e2e-8cb5-101bee0353dc` (optional, string) - Lists only measurements from container with this ID
    + knskoId: `19196` (optional, string) - Lists only measurements from container with this KNSKO ID.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + from: `2019-05-16T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime
    + to: `2019-05-18T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Sorted Waste Measurement)

### GET All Sorted Waste Picks [GET /sortedwastestations/picks/{?containerId,limit,offset,from,to,knskoId}]

+ Parameters

    + containerId: `1a90b0e0-fabb-5e2e-8cb5-101bee0353dc` (optional, string) - Lists only picks from container with this ID
    + knskoId: `19196` (optional, string) - Lists only picks from container with this KNSKO ID.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + from: `2019-05-16T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured from this datetime
    + to: `2019-05-18T04:27:58.000Z` (optional, string) - Date in ISO8601, limits data measured up until this datetime

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Sorted Waste Pick)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

## Waste Collection Yards [/wastecollectionyards]

![golemioapi-opendata-badge](https://img.shields.io/badge/opendata-available-green)

### GET All Waste Collection Yards [GET /wastecollectionyards/{?latlng,range,districts,limit,offset,updatedSince}]

+ Parameters

    + latlng: `50.124935,14.457204` (optional, string) - Sorting by location (Latitude and Longitude separated by comma, latitude first).
    + range: 5000 (optional, number) - Filter by distance from latlng in meters (range query). Depends on the latlng parameter.
    + districts: `praha-4` (optional, array) - Filter by Prague city districts (slug) separated by comma.
    + limit: 10 (optional, number) - Limits number of retrieved items. The maximum is 10000 (default value).
    + offset: 0 (optional, number) - Number of the first items that are skipped.
    + updatedSince: `2019-05-18T07:38:37.000Z` (optional, string) - Filters all results with older updated_at than this parameter

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (object)
        + type: FeatureCollection (string, required)
        + features (array)
            + (Feature Point)
                + properties (Waste Collection Yard)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET Waste Collection Yard [GET /wastecollectionyards/{id}]

+ Parameters

    + id: `sberny-dvur-hlavniho-mesta-prahy-probostska` (string) - Identifier of the Waste Collection Yard.

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (Feature Point)
        + properties (Waste Collection Yard)

+ Response 404
    + Attributes (Error)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)

### GET All Waste Collection Yards Properties [GET /wastecollectionyards/properties]

+ Request (application/json; charset=utf-8)

    + Headers

            x-access-token: YOUR_ACCESS_TOKEN

+ Response 200 (application/json; charset=utf-8)

    + Attributes (array)
        + (Waste Collection Yard Property)

+ Response 403 (application/json; charset=utf-8)
    + Attributes (403 Error)


# Data structure

## Point (array[number])

+ 14.4633 (number, required) - longitude
+ 50.07827 (number, required) - latitude

## Polygon (array[Point])

+ (Point, required)
+ (Point, required)
+ (Point, required)
+ (Point, required) - Four points make basic polygon
+ (Point, optional)

## Multi Polygon (array[Polygon])

+ (Polygon, required) - First list is an enclosing polygon
+ (Polygon, optional) - Next ones are cut out areas

## Error (object)
+ error_message: `Not Found` (required, string)
+ error_status: 404 (required, number)

## 403 Error (object)
+ error_message: `Forbidden` (required, string)
+ error_status: 403 (required, number)

## Feature Point (object)

+ geometry (required)
    + type: Point (string, required)
    + coordinates (Point, required)
+ properties (object, required)
+ type: Feature (string, required)

## Feature Polygon (object)

+ geometry (required)
    + type: Polygon (required, string)
    + coordinates (array[Polygon], required)
+ properties (object, required)
+ type: Feature (required, string)

## Address (object)

+ address_formatted: `Dělnická 213/10, 17000 Praha-Holešovice, Česko` (string)
+ street_address: `Dělnická 213/10` (string)
+ postal_code: `17000` (string)
+ address_locality: `Praha` (string)
+ address_region: `Holešovice` (string)
+ address_country: `Česko` (string)

## Air Quality Station (object)

+ id: ACHOA (string, required)
+ district: `praha-11` (string)
+ measurement
  + AQ_hourly_index: 3 (number)
  + components (array[Air Quality Station Component])
+ name: `Praha 4-Chodov` (string, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)

## Air Quality Station Component (object)

+ averaged_time
  + averaged_hours: 1 (number)
  + value: 10.7 (number),
+ type: NO2 (string)

## Air Quality Station History (object)

+ id: ACHOA (string, required)
+ measurement
  + AQ_hourly_index: 3 (number)
  + components (array[Air Quality Station Component])
+ updated_at: `2019-05-18T07:38:37.000Z` (string)

## Bicycle Counter (object)

+ id: `camea-BC_AT-STLA` (string, required)
+ name: `Altetická` (string, required)
+ route: `A140` (string)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ directions (array[Bicycle Counter Direction])

## Bicycle Counter Detections (object)

+ id: `camea-BC_ZA-BO` (string) - direcrion_id
+ value: 3 (number)
+ value_pedestrians: 7 (number)
+ locations_id: `camea-BC_PN-VYBR` (string)
+ measured_from: `2019-05-18T07:38:37.000Z` (string)
+ measured_to: `2019-05-18T07:38:37.000Z` (string)
+ measurement_count: `1` (number)

## Bicycle Counter Temperatures (object)

+ id: `camea-BC_AT-STLA` (string) - location_id
+ value: 3 (number)
+ measured_from: `2019-05-18T07:38:37.000Z` (string)
+ measured_to: `2019-05-18T07:38:37.000Z` (string)
+ measurement_count: `1` (number)

## Bicycle Counter Direction (object)

+ id: `BC_AT_ST` (string, required) - direction ID
+ name: `Strahov` (string) - direction name

## Bicycle Parking (object)

+ district: `praha-4` (string)
+ id: 6239570526 (number, required)
+ tags (array[Bicycle Parking Tag])
+ updated_at: `2019-05-18T07:38:37.000Z` (string)

## Bicycle Parking Tag (object)

+ id: `capacity` (string, required)
+ value: `2` (string)

## City District (object)

+ id: 538124 (number, required)
+ name: `Praha-Březiněves` (string, required)
+ slug: `praha-brezineves` (string, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)

## Departure Board (object)

+ delay (Departure Board Delay)
+ arrival_timestamp (Departure Board Stop Time)
+ departure_timestamp (Departure Board Stop Time)
+ route (Departure Board Route)
+ stop (Departure Board Stop)
+ trip (Departure Board Trip)

## Departure Board Trip (object)

+ headsign: `Chodov` (string, required) - Trip headsign.
+ id: `140_224_200302` (string, required) - GTFS trip id.
+ is_canceled: `false` (boolean, required) - If the trip is canceled.
+ is_wheelchair_accessible: `1` (number, required) - If the vehicle is wheelchair accessible.

## Departure Board Stop (object)

+ name: `Prosecká` (string, required) - Name of station.
+ id: `U754Z1P` (string, required) - GTFS station id.
+ platform_code: `A` (string, required) - Station platform.
+ is_wheelchair_accessible: `1` (number, required) - 1 for wheelchair accessible stop.

## Departure Board Delay (object)

+ is_available: `false` (boolean, required) - If true, is avalable information about trip's realtime delay.
+ minutes: `3` (string, optional) - Delay rounded to minutes.
+ seconds: `176` (string, optional) - Delay in seconds.

## Departure Board Stop Time (object)

+ predicted: `2019-05-18T07:38:20.000Z` (string, required) - Time of arrival/departure including realtime delay in ISO String (UTC).
+ scheduled: `2019-05-18T07:38:00.000Z` (string, required) - Time of arrival/departure in ISO String (UTC).

## Departure Board Route (object)

+ name: `140` (string, required) - Route short name.
+ type: `3` (number, required) - Identifier of the route type in the GTFS static feed.
+ `mpv_type`: `1` (number, required) - Identifier of the vehicle type in the MPVNet.


## ASW id (object)

+ `node`: 100 (number, required) - ASW ID of node.
+ `stop`: 2 (number, required) - Identification of the stop within the node.

## PID Departure Board (object)

+ stops (array) - List of stops in the requested set and their properties.
    + (PID Departure Board Stop)
+ departures (array) - List of departures.
    + (PID Departure Board Departure)
+ infotexts (array) - List of active infotexts for selected stops.
    + (PID Departure Board Infotext)

## PID Departure Board Departure (object)

+ arrival_timestamp (PID Departure Board Stop Time)
+ delay (PID Departure Board Delay)
+ departure_timestamp (PID Departure Board Stop Time)
+ last_stop (PID Departure Board Last Stop)
+ route (PID Departure Board Route)
+ stop (PID Departure Board Stop Reference)
+ trip (PID Departure Board Trip)


## PID Departure Board Infotext (object)

+ valid_from: `2019-05-18T07:38:00.000Z` (string, required, nullable) - Intended time of infotext publishing in ISO String (UTC). Does not ahve to represent the time the reported event starts.
+ valid_to: `2019-05-19T07:38:00.000Z` (string, required, nullable) - Intended time of infotext removal in ISO String (UTC). Does not have to represent the time the reported event ends.
+ text: `Nehoda na trase, odklon linek mimo tuto stanici.` (string, required) - Information text in Czech.
+ text_en: `Trips are cancelled due to accident on route.` (string, required, nullable) - Information text in English.
+ display_type: `inline` (string, required) - Type of display on board by the intended form of presentation (where applicable). Enumerating `inline` (to be presented along with departures, usually in a marquee), `general` (to be displayed full screen instead of departures), `general-alternate` (full screen alternates with departures).
+ related_stops (array)
    + `U118S1` (string) - GTFS station IDs for which the infotexts apply.

## PID Departure Board Trip (object)

+ headsign: `Chodov` (string, required) - Trip headsign (usually the final stop).
+ id: `140_224_200302` (string, required) - GTFS trip ID.
+ is_canceled: `false` (boolean, required) - True if the trip is canceled.
+ is_wheelchair_accessible: `false` (boolean, required) - True if the vehicle is wheelchair accessible.

## PID Departure Board Last Stop (object)

+ name: `Prosecká` (string, required) - Name of the last stop the vehicle reported from.
+ id: `U754Z1P` (string, required) - GTFS ID of the stop.

## PID Departure Board Stop (object)

+ `level_id`: U118L2 (string, required) - Level of metro stops.
+ `location_type`: 0 (number, required) - Describes the type of stop, is always `0`.
+ `parent_station`: U118S1 (string, required)
+ `platform_code`: A (string, required) - Human-friendly code used to distinguish a stop inside a node. Multiple GTFS codes can share a platform code.
+ `stop_lat`: 50.4 (number, required) - Latitude of the stop marker or the center of metro or train station platform.
+ `stop_lon`: 14.4 (number, required) - Longitude of the stop marker or the center of metro or train station platform.
+ `asw_id` (ASW id, required, nullable) - ASW ID of the stop.
+ `stop_id`: U118Z101P (string, required) - GTFS ID of the stop.
+ `stop_name`: Flora (string, required) - Name of the stop.
+ `wheelchair_boarding`: 2 (number, required) - Indicates accessibility of the stop by wheelchairs. `0` – unknown, `1` – accessible, `2` – inaccessible
+ `zone_id`: P (string, required) - PID fare zone. Multiple zones are separated by a comma.

## PID Departure Board Stop Reference (object)

+ id: `U754Z1P` (string, required) - GTFS ID of the stop the vehicle is stopping at.

## PID Departure Board Delay (object)

+ is_available: `false` (boolean, required) - True if information about trip's delay is available.
+ minutes: `3` (string, optional) - Delay rounded to minutes.
+ seconds: `176` (string, optional) - Delay in seconds.

## PID Departure Board Stop Time (object)

+ predicted: `2019-05-18T07:38:20.000Z` (string, required) - Time of arrival/departure including realtime delay in ISO String (UTC).
+ scheduled: `2019-05-18T07:38:00.000Z` (string, required) - Time of arrival/departure in ISO String (UTC).

## PID Departure Board Route (object)

+ short_name: `140` (string, required) - Route designation used by PID.
+ type: `3` (string, required) - GTFS route type. `0` – tram, `1` – metro, `2` – train, `3` – bus, `4` – ferry, `7` – funicular, `11` – trolleybus

## Garden (object)

+ address (Address)
+ description: `Mozartova busta v zahradě legendární usedlosti stojí na vyvýšeném, trochu zvláštním, místě, jakoby stranou pomyslné hlavní osy podlouhlého pozemku. Mistr, byť pouze v kamenné podobě, tak má dokonalý přehled o dění v zahradě, ale současně překrásný výhled na stráně vrchu Mrázovka.` (string)
+ district: `praha-5` (string)
+ id: `betramka` (string, required)
+ image (Garden Image)
+ name: `Betramka` (string, required)
+ properties (array[Garden Property])
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ url: `http://www.praha.eu/jnp/cz/co_delat_v_praze/parky/bertramka/bertramka_text.html` (string)

## Garden Image (object)

+ url: `http://www.praha.eu/public/5c/7c/31/96820_4_bertramka_08.jpg` (string)

## Garden Property (object)

+ id: `doba` (string, required)
+ description: `Otevírací doba` (string, optional)
+ value: `Celoročně duben až říjen 9-18 hod., listopad až březen 9:30-16 hod.` (string)

## GTFS Trip (object)

+ `trip_id`: 991_1165_190107 (string, required)
+ bikes_allowed: 1 (number, required)
+ `block_id`: ` ` (string, optional)
+ created_time: `2018-09-09 12:32:45` (string, nullable)
+ direction_id: 0 (number, optional)
+ exceptional: 0 (number)
+ last_modify: `2018-09-09 12:32:45` (string, optional, nullable)
+ route_id: L991 (string, required)
+ service_id: `1111100-1` (string, required)
+ shape_id: L991V3 (string, required)
+ trip_headsign: `Nemocnice Motol` (string, required)
+ wheelchair_accessible: 1 (number, required)

## GTFS Stop (object)

+ `location_type`: 0 (number, required)
+ `parent_station`: U118S1 (string, required)
+ `platform_code`: A (string, required)
+ `stop_id`: U118Z101P (string, required)
+ `stop_name`: Flora (string, required)
+ `wheelchair_boarding`: 2 (number, required)
+ `zone_id`: P (string, required)

## GTFS Stop time (object)

+ `arrival_time`: `13:12:05` (string, required)
+ `arrival_time_seconds` (number, optional, nullable)
+ `created_time`: `2018-09-09 12:32:45` (string, required)
+ `departure_time`: `13:12:35` (string, required)
+ `departure_time_seconds` (number, optional, nullable)
+ `drop_off_type`: 0 (number, required)
+ `last_modify`: `2018-09-09 12:32:45` (string, optional, nullable)
+ `pickup_type`: 0 (number, required)
+ `shape_dist_traveled`: 3.73622 (number, required)
+ `stop_headsign`: ` ` (string, optional)
+ `stop_id`: U921Z102P (string, required)
+ `stop_sequence`: 4 (number, required)
+ `trip_id`: `991_1156_180709` (string, required)
+ `stop` (GTFS Stop)

## GTFS Shape (object)

+ `shape_dist_traveled`: 16.46309 (number, required))
+ `shape_id`: L991V2 (string, required)
+ `shape_pt_sequence`: 123 (number, required)

## GTFS Service (object)

+ `created_time`: `2018-09-09 12:32:45` (string, required)
+ `end_date`: 20190311 (string, required)
+ `friday`: 1 (number, required)
+ `last_modify`: `2018-09-09 12:32:45` (string, optional, nullable)
+ `monday`: 1 (number, required)
+ `saturday`: 1 (number, required)
+ `service_id`: `1111111-1` (string, required)
+ `start_date`: 20190226 (string, required)
+ `sunday`: 1 (number, required)
+ `thursday`: 1 (number, required)
+ `tuesday`: 1 (number, required)
+ `wednesday`: 1 (number, required)

## GTFS Route (object)

+ `agency_id`: `99` (string, required)
+ `created_time`: `2018-09-09 12:32:45` (string, optional, nullable)
+ `is_night`: `false` (boolean, required)
+ `is_regional`: `false` (boolean, required)
+ `is_substitute_transport`: `false` (boolean, required)
+ `last_modify`: `2018-09-09 12:32:45` (string, optional, nullable)
+ `route_color`: 00A562 (string, required)
+ `route_desc`: null (string, nullable)
+ `route_id`: L991 (string, required)
+ `route_long_name`: `Nemocnice Motol - Petřiny - Skalka - Depo Hostivař` (string, required)
+ `route_short_name`: A (string, required)
+ `route_text_color`: FFFFFF (string, required)
+ `route_type`: `1` (string, required)
+ `route_url`: `https://pid.cz/linka/A` (string, required)

## Medical Institution (object)

+ address (Address)
+ district: `praha-4` (string)
+ email (array[string])
+ id: `04995142000-benu-lekarna` (string, required)
+ institution_code: `04995142000` (string, required)
+ name: `BENU Lékárna` (string, required)
+ opening_hours (array[Opening Hours])
+ pharmacy_code: `04996100` (string)
+ telephone (array[string])
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ type (Medical Institution Type)
+ web (array[string])

## Medical Institution Address (object)

+ city: `Praha 4` (string, required)
+ formatted: `Olbrachtova 2006/9, 140 00 Praha 4` (string, required)
+ street: `Olbrachtova 2006/9` (string, required)
+ zip: `140 00` (string, required)

## Medical Institution Type (object)

+ description: `Lékárna` (string)
+ group: `pharmacies` (string)
+ id: `Z` (string, required)

## Meteosensor (object)

+ air_temperature: 18.95 (number)
+ humidity: 31 (number)
+ id: 531802 (number, required)
+ last_updated: 1556806121000 (number, required)
+ name: `Bulhar` (string, required)
+ road_temperature: 34.55 (number)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ wind_direction: 230 (number)
+ wind_speed: 2.08 (number)
+ district: `praha-2` (string)

## Municipal Authority (object)

+ image (Municipal Authority Image)
+ email (array[string])
+ telephone (array[string])
+ web (array[string])
+ address (Address)
+ agendas (array[Municipal Authority Agenda])
+ district: `praha-1` (string)
+ id: `magistrat-hlavniho-mesta-prahy` (string, required)
+ name: `Magistrát hlavního města Prahy` (string, required)
+ official_board: `http://www.praha.eu/jnp/cz/o_meste/magistrat/deska/index.html` (string)
+ opening_hours (array[Opening Hours])
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ type (Municipal Authority Type)

## Municipal Authority Type (object)

+ description: `Obecní úřad` (string)
+ id: `municipality` (string, required)

## Municipal Authority Image (object)

+ mimetype: `image/png` (string)
+ size: 714603 (number)
+ url: https://www.mojepraha.eu/images/municipal-authorities/VJXPN4RVrTLnkih5.png (string)

## Municipal Authority Agenda (object)

+ keywords (array[string])
+ description: `Pronájem bytových a nebytových prostor` (string)
+ long_description (string)

## Municipal Authority Queues (object)

+ last_updated: `2019+04-30T09:56:00+02:00` (string, required)
+ municipal_authority_id: `skoduv-palac` (string, required)
+ served_activities (array[Municipal Authority Activities])
+ updated_at: `2019-04-30T09:56:00+02:00` (string, required)
+ title: `Monitoring odbavování klientů ve Škodově paláci` (string, required)

## Municipal Authority Activities (object)

+ activity: `OSV: CzechPoint` (string, required)
+ number_of_person_in_queue: 0 (number, required)
+ number_of_serving_counters: 5 (number, required)

## Municipal Library (object)

+ address (Address)
+ district: `praha-1` (string)
+ id: 2 (number, required)
+ email: `knihovna@mlp.cz` (string, required)
+ name: `Barrandov` (string, required)
+ opening_hours (array[Opening Hours])
+ sections_and_departments (array[Municipal Library Section])
+ services (array[Municipal Library Service])
+ telephone: `+420 770 130 262` (string, required)
+ updated_at: `2020-06-30T14:16:45.891Z` (string, required)
+ web: `https://www.mlp.cz/cz/pobocky/barrandov/` (string, required)

## Municipal Library Section

+ id: `code-8` (string, required)
+ name: `Oddělení pro děti a mládež` (string, required)
+ type: `tag` (string, optional, nullable)
+ url (string, optional, nullable)

## Municipal Library Service

+ description: `Bezbariérový přístup pro vozíčkáře i rodiče s kočárky` (string, optional, nullable)
+ id: 3 (number, required)
+ name: `Bezbariérový přístup` (string, required)

## Municipal Police Station (object)

+ cadastral_area: `Staré Město` (string, required)
+ id: `stare-mesto-lodecka-2` (string, required)
+ note: `Přestupky` (string)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ district: `praha-1` (string)
+ address (Address)

## Opening Hours (object)

+ closes: `12:00` (string)
+ day_of_week: `Monday` (string)
+ description (string)
+ is_default (boolean)
+ opens: `08:00` (string)
+ valid_from: `2020-06-30T22:00:00.000Z` (string, optional, nullable)
+ valid_through: `2020-08-30T22:00:00.000Z` (string, optional, nullable)

## Parking (object)

+ parking_type (Parking Type)
+ id: 534012 (number, required)
+ name: `Skalka 1` (string, required)
+ num_of_free_places: 0 (number, required)
+ num_of_taken_places: 63 (number, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ total_num_of_places: 63 (number, required)
+ average_occupancy (Parking Occupancy)
+ district: `praha-10` (string)
+ address (Address)
+ last_updated: 1556868553000 (number)
+ payment_link: `https://ke-utc.appspot.com/static/onstreet.html?shortname=143` (string, optional, nullable)
+ payment_shortname: `143` (string, optional, nullable)

## Parkings History (object)

+ id: 534012 (number, required)
+ num_of_free_places: 0 (number, required)
+ num_of_taken_places: 63 (number, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string) - timestamp of requesting vendor API
+ total_num_of_places: 63 (number, required)
+ last_updated: 1556868553000 (number) - timestamp of measurement by vendor

## Parking Occupancy (object)

+ 0 (Parking Occupancy Day)
+ 1 (Parking Occupancy Day)
+ 2 (Parking Occupancy Day)
+ 3 (Parking Occupancy Day)
+ 4 (Parking Occupancy Day)
+ 5 (Parking Occupancy Day)
+ 6 (Parking Occupancy Day)

## Parking Occupancy Day
+ 00: 32 (number)
+ 01: 57 (number)
+ 02: 17 (number)
+ 03: 47 (number)
+ 04: 117 (number)
+ 05: 5 (number)
+ 06: 59 (number)
+ 07: 59 (number)
+ 08: 159 (number)
+ 09: 59 (number)
+ 10: 59 (number)
+ 11: 59 (number)
+ 12: 59 (number)
+ 13: 59 (number)
+ 14: 59 (number)
+ 15: 59 (number)
+ 16: 99 (number)
+ 17: 59 (number)
+ 18: 59 (number)
+ 19: 59 (number)
+ 20: 67 (number)
+ 21: 32 (number)
+ 22: 59 (number)
+ 23: 59 (number)

## Parking Type (object)

+ description: `P+R parkoviště` (string)
+ id: 1 (number, required)

## Parking Zone (object)

+ id: `P1-0111` (string, required)
+ midpoint: 14.3933334097666, 50.0906432052142 (array[number], required)
+ name: `Rezidentní úsek - P1-0111` (string, required)
+ northeast: 14.3933334097666, 50.0906432052142 (array[number], required)
+ number_of_places: 31 (number, required)
+ payment_link: `https://ke-utc.appspot.com/static/onstreet.html?shortname=P1-0111` (string)
+ southwest: 14.3933334097666, 50.0906432052142 (array[number], required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ type (Parking Zone Type)
+ zps_id: 3312 (number, optional, nullable)
+ zps_ids (array[number], optional)
+ tariff_ids: `d1bc6cae620aa65200d47b61cf483299ca03d6c9` (array[string])
+ tariffs_text: `Po-Ne 08:00-19:59 80 Kč/hod. (max. 1 hod.); Po-Ne 20:00-05:59 80 Kč/hod. (max. 1 hod.); Po-Ne (ve svátek) 08:00-05:59 80 Kč/hod. (max. 1 hod.)` (string)

## Parking Zone Tariff Schedule (object)

+ days: Saturday, Sunday (array[string])
+ tariff (array[Parking Zone Tariff])

## Parking Zone Tariff (object)

+ time_from: `PT8H` (string)
+ pay_at_holiday: `false` (boolean)
+ max_parking_time: `PT1H` (string)
+ time_to: `PT19H59M` (string)
+ price_per_hour: 80 (number)
+ max_price: 0 (number)
+ divisibility: `PT15M` (string)
+ tariff_id: `d1bc6cae620aa65200d47b61cf483299ca03d6c9` (array[string])

## Parking Zone Type (object)

+ description: `Rezidentní úsek` (string)
+ id: 1 (number, required)

## Parking Space (object)

+ id: `6` (string, required)
+ source: `korid` (string, required)
+ sourceId: `33` (string, required)
+ dataProvider: `www.korid.cz` (string, nullable)
+ name: `Husova (u nemocnice)` (string, required)
+ category: `park_and_ride` (string, nullable)
+ dateModified: `2020-05-18T07:38:37.000Z` (string, required)
+ address: null (string, nullable)
+ areaServed: null (string, nullable)
+ paymentUrl: `https://payment.com/link.html` (string, nullable)
+ totalSpotNumber: 180 (number, required)
+ tariffId: 20 (number, nullable)
+ validFrom: `2020-05-31T00:00:00.000Z` (string, nullable)
+ validTo: `2022-05-31T00:00:00.000Z` (string, nullable)

## Parking Measurement (object)

+ id: `10168955` (string, required)
+ source: `korid` (string, required)
+ sourceId: `20` (string, required)
+ availableSpotNumber: 5 (number, required)
+ closedSpotNumber: 0 (number, required)
+ occupiedSpotNumber: 8 (number, required)
+ totalSpotNumber: 13 (number, required)
+ dateModified: `2021-02-26T12:02:46.188Z` (string, required)

## Parking Space Tariff Charge (object)
+ charge: 20 (number, required)
+ chargeOrderIndex: 0 (number, required)
+ chargeType: `minimum` (string, nullable)
+ chargeInterval: 1800 (number, required)
+ maxIterationsOfCharge: 1 (number, nullable)
+ minIterationsOfCharge: 1 (number, nullable)
+ startTimeOfPeriod: `Mon-Fri 09:00` (string, nullable)
+ endTimeOfPeriod: `Mon-Sun 14:30` (string, nullable)

## Parking Space Tariff (object)

+ tariffId: `b184865e-5a5e-5465-abc1-4fab6bc56a77` (string, required)
+ source: `korid` (string, required)
+ lastUpdated: `2020-10-12T15:19:21+02:00` (string, required)
+ paymentMode: `` (string, required)
+ paymentAdditionalDescription: `Parkování Liberec` (string, nullable)
+ freeOfCharge: false (boolean, required)
+ urlLinkAddress: `https://parking.liberec.cz/` (string, required)
+ chargeBandName: `A` (string, required)
+ acceptsPaymentCard: true (boolean, nullable)
+ acceptsCash: false (boolean, nullable)
+ acceptsMobilePayment: false (boolean, nullable)
+ chargeCurrency: `CZK` (string, required)
+ allowedVehicleType: `carSharing, taxi` (string, nullable)
+ allowedFuelType: `electricity, petrol, lpg` (string, nullable)
+ charge: (array[Parking Space Tariff Charge])

## Playground (object)

+ image (Playground Image)
+ content: `Popis: Asi 300 m od prodejny Lidl v Hornoměcholupské ulici se na okraji louky rozkládá první hřiště (43.A). Tvoří ho...` (string, required)
+ id: 72 (number, required)
+ name: `Hostivařský lesopark (východní část) - hřiště 43.B` (string, required)
+ perex: `Trasa je vhodným polodenním rodinným výletem.` (string)
+ properties (array[Playground Property])
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ url: `http://www.hristepraha.cz/hriste/mapa/hostivarsky-lesopark-vychodni-cast-hriste-43-b` (string, required)
+ district: `praha-15` (string)
+ address (Address)

## Playground Image (object)

+ url: `http://www.hristepraha.cz/images/img/2d73f6832f5ce39fc5987d27d9f4541fo.jpg` (string)

## Playground Property (object)

+ id: 6 (number, required)
+ description: `WC na hřišti nebo v bezprostřední blízkosti` (string)

## Public Toilet (object)

- id: 18 (number, required)
- opened: `10:00 - 21:00` (string)
- price: `zdarma` (string)
- updated_at: `2019-05-18T07:38:37.000Z` (string)
- district: `praha-13` (string)
- address (Address)

## Shared Bike (object)

+ company (Shared Bike Company)
+ estimated_trip_length_in_km: 41 (number)
+ id: `homeport-0223` (string, required)
+ in_rack: true (boolean)
+ label (string, required)
+ location_note (string, nullable)
+ name: `0223` (string)
+ res_url: `https://freebike.app.link?bikeId=0223&system=praha` (string, required)
+ type (Shared Bike Type)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)

## Shared Bike Company (object)

+ email (string, nullable)
+ name: `HOMEPORT` (string, required)
+ phone (string, nullable)
+ web: `prague.freebike.com` (string, required)

## Shared Bike Type (object)

+ description: `ebike` (string)
+ id: 2 (number, required)

## Shared Car (object)

+ availability (Shared Car Availability)
+ company (Shared Car Company)
+ fuel (Shared Car Fuel)
+ id: `9B99149` (string, required)
+ name: `Škoda Citigo` (string, required)
+ res_url: `https://autonapul.zemtu.com/app/#/?rz=9B99149` (string, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)

## Shared Car Availability (object)

+ description: `ihned` (string)
+ id: 1 (number, required)

## Shared Car Company (object)

+ email: `autonapul@autonapul.cz` (string)
+ name: `Autonapůl` (string, required)
+ phone: `+420 778 470 898` (string)
+ web: `https://www.autonapul.cz/` (string, required)

## Shared Car Fuel (object)

+ description: `benzín` (string)
+ id: 1 (number, required)

## Sorted Waste Station (object)

+ accessibility (Sorted Waste Station Accessibility)
+ containers (array[Sorted Waste Station Container])
+ district: `praha-13` (string)
+ id: `dd08a6a7-1241-568a-9b1b-f384d69c358a` (string, required)
+ name: `Sběrný dvůr hl. m. Prahy – areál skládky Ďáblice` (string, required)
+ station_number: `0001/ 001` (string)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ is_monitored: true (boolean, optional) - contains at least one container which is monitored with sensor
+ knsko_id: 3497 (number)

## Sorted Waste Station Accessibility (object)

+ description: `Příjem odpadu` (string, required)
+ id: 1 (number, required)

## Sorted Waste Station Container (object)

+ cleaning_frequency (Sorted Waste Station Container Cleaning Frequency)
+ company (Sorted Waste Station Container Company)
+ container_type: 3000 L Podzemní - SV (string)
+ description (string)
+ trash_type (Sorted Waste Station Container Trash Type)
+ last_measurement (Sorted Waste Station Last Measurement, optional)
+ last_pick (Sorted Waste Station Last Pick, optional)
+ knsko_id: 15288 (number)
+ container_id: `dc532519-f2bd-5ab2-b049-132bad684dab` (string, optional)
+ sensor_code: `0006/ 016C00214` (string, optional)
+ sensor_supplier: `Sensoneo` (string, optional)

## Sorted Waste Station Container Cleaning Frequency (object)

+ id: 21 (number)
+ duration: `P2W` (string)
+ frequency: 1 (number)

## Sorted Waste Station Container Company (object)

+ email (string)
+ name (string)
+ phone (string)
+ web (string)

## Sorted Waste Station Last Pick (object)

+ pick_at_utc: `2019-07-09T09:00:37.000Z` (string)

## Sorted Waste Station Last Measurement (object)

+ measured_at_utc: `2019-08-27T14:00:32.000Z` (string)
+ percent_calculated: 62 (number)
+ prediction_utc: `2019-05-18T07:38:37.000Z` (string)

## Sorted Waste Station Container Trash Type (object)

+ description: `Čiré sklo` (string, required)
+ id: 7 (number, required)

## Sorted Waste Pick (object)

+ id: 12570452 (number, required)
+ container_id: 30162 (number, required)
+ pick_minfilllevel: 30 (number)
+ decrease: 20 (number)
+ code: `0005/ 025C00155` (string, required)
+ pick_at_utc: `2019-05-18T04:27:58.000Z` (string, required)
+ percent_before: 61 (number)
+ percent_now: 0 (number)
+ event_driven: false (boolean)

## Sorted Waste Measurement (object)

+ id: `19fb006c-b3c3-5876-a7d8-8fa9520cd747` (string, required)
+ container_id: `7089c1ba-c2a4-52e9-bd54-9448bfdd7afe` (string, required)
+ code: `0009/ 157C00353` (string, required)
+ percent_calculated: 67 (number, required)
+ upturned: 0 (number)
+ temperature: 15 (number)
+ battery_status: 3.76 (number)
+ measured_at_utc: `2019-05-17T15:11:27.000Z` (string, required)
+ prediction_utc: `2019-05-18T07:38:37.000Z` (string) - vendor prediction of maximum fullness
+ firealarm: 0 (number)

## Traffic Camera (object)

+ image (Traffic Camera Image)
+ id: 606748 (number, required)
+ last_updated: 1557229128000 (number, required) - timestamp of last update by vendor
+ name: `DP - Kobylisy C - BUS MHD DP` (string, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string) - timestamp of requesting vendor API
+ district: `praha-8` (string)
+ address (Address)

## Traffic Camera Image (object)

+ data: `data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQAB.........pgf//Z` (string)
+ file_size: 20072 (number, required)
+ type: `image/jpeg` (string)
+ url: `http://www.tsk-praha.cz/cams/cam606748.jpg?1557229128000` (string, required)

## Vehicleposition Compound (object)
+ `trip` (Vehicleposition Trip) - Trip properties
+ `last_position` (Vehicleposition Position) - Properties of last known position
+ `all_positions` (object) - All known positions with their properties
    + type: FeatureCollection (string, required)
    + features (array)
        + (Feature Point)
            + properties (Vehicleposition Position)

## Vehicleposition Trip (object)

+ `agency_name` (object)
    + `real`: `OAD KO Kolín` (string, required) - Agency name that normally operates the trip
    + `scheduled`: `OAD KO Kolín` (string, required) - Agency name that currently operates the trip (normally identical to planned_agency_name)
+ `cis` (object) - All properties that come from nation central information system
    + `line_id`: `100381` (string, required) - Full line number
    + `trip_number`: 29 (number, required) - Trip number (unique within specific cis_line_id)
+ `sequence_id`: 1 (number, required) - Identifier of trip sequence operated by a single vehicle
+ `origin_route_name`: 381 (string, required) - Name of the route from which the vehicle originates
+ `gtfs` (object) - All properties tied with GTFS data format
    + `route_id`: L381 (string, required) - Identifier of the owner route in the GTFS static feed
    + `route_short_name`: 381 (string, required) - Identification of the line used for the public
    + `route_type`: `1` (number, required) - Identifier of the route type in the GTFS static feed
    + `trip_id`: `381_51_180728` (string, required) - Identifier of the trip in the GTFS static feed
    + `trip_headsign`: `Sídliště Modřany` (string, optional, nullable) - Heading of the trip (identical to what is shown on the bus)
+ `start_timestamp`: `2019-03-18T11:00:00.000Z` (string, required) - Timestamp of arrival time in first stop (UTC)
+ `vehicle_type` (Vehicleposition Vehicle Type) - Type of vehicle
+ `vehicle_registration_number`: 8635 (number, required) - Four-digit identifier of the vehicle in the system
+ `wheelchair_accessible`: true (boolean, required) - Determines wheter the vehicle has wheelchair access

## Vehicleposition Vehicle Type (object)
+ `id`: 1 (number, required) - Unique id code of type
+ `description_cs`: `Autobus` (string, required) - Description of type in Czech
+ `description_en`: `Bus` (string, required) - Description of type in English

## Vehicleposition Position (object)

+ `bearing`: 34 (number, required, nullable) - Bearing, in degrees (0 = north, 90 = east, 180 = south, 270 = west)
+ `delay` (object) - Calculated delay for this position
    + `actual`: 213 (number, optional, nullable) - Current delay, in seconds, based on actual location of a trip on its shape between stops
    + `last_stop_arrival`: 23 (number, optional, nullable) - Delay, in seconds, at the last confirmed stop on arrival. Null, if the arrival time to the last stop equal departure time
    + `last_stop_departure`: `-84` (number, optional, nullable) - Delay, in seconds, at the last confirmed stop on departure. Null, if it was a terminal stop.
+ `last_stop` (object) - Properties for last stop passed
    + `id`: 331 (string, required, nullable) - GTFS Static identifier of the last stop passed on the route of the trip
    + `sequence`: 11 (number, required, nullable) - Sequence number of the last stop passed on the route of the trip
    + `arrival_time`: `2019-03-18T11:00:00.000Z` (string, required, nullable) - Scheduled arrival time to the last passed stop (UTC)
    + `departure_time`: `2019-03-18T11:04:00.000Z` (string, required, nullable) - Scheduled departure time from the last passed stop (UTC)
+ `next_stop` (object) - Properties of the next stop on the route of the trip
    + `id`: 334 (string, required, nullable) - GTFS Static identifier of the next stop on the route of the trip
    + `sequence`: 12 (number, required, nullable) - Sequence number of the next stop on the route of the trip
    + `arrival_time`: `2019-03-18T11:10:00.000Z` (string, required, nullable) - Scheduled arrival time of the next stop on the route of the trip
    + `departure_time`: `2019-03-18T11:14:00.000Z` (string, required, nullable) - Scheduled departure time of the next stop on the route of the trip
+ `is_canceled`: false (boolean, nullable, required) - True, if the trip was canceled. Other fields about the position have non-defined values in case this value is true
+ `origin_timestamp`: `2019-03-18T11:00:00.000Z` (string, required) - Time at which the position was sent from the vehicle (UTC)
+ `speed`: 34 (number, optional, nullable) - Current speed of the vehicle (kilometers per hour)
+ `shape_dist_traveled`: `12.3` (string, optional, nullable) - Number of kilometers traveled on the route
+ `tracking`: false (boolean, required) - True if trip is tracked, meaning that trip is on its track.


## Waste Collection Yard (object)

+ properties (array[Waste Collection Yard Property])
+ address (Address)
+ contact: `236 04 00 11, 236 04 00 00` (string, required)
+ id: `sberny-dvur-hlavniho-mesta-prahy-probostska` (string, required)
+ name: `Sběrný dvůr hlavního města Prahy Proboštská` (string, required)
+ operating_hours: `Po - Pá 8:30 - 18:00 hod., 8:30 - 17:00 hod. v zimním období, So 8:30 - 15:00 hod.` (string, required)
+ operator: `KOMWAG, a.s.` (string, required)
+ updated_at: `2019-05-18T07:38:37.000Z` (string)
+ type: `SDHMP` (string, required)
+ district: `praha-8` (string)

## Waste Collection Yard Property (object)

+ `description`: `Příjem odpadu` (string)
+ `id`: `ODPADPRIJEM` (string)
+ `value`: `objemný odpad (nábytek, zařízení domácnosti), kovy, dřevo, odpad z údržby zeleně, suť z bytových úprav, papír, sklo, plasty, nebezpečné odpady, pneumatiky, použitý textil, obuv a oděvy` (string)
